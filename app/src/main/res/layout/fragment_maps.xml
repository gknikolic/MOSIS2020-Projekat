<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="@dimen/fragmentPadding">

    <Spinner
        android:id="@+id/mapsFragment_caseTypeSpinner"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginBottom="@dimen/fab_margin" />

    <Spinner
        android:id="@+id/mapsFragment_markersSpinner"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@+id/mapsFragment_caseTypeSpinner"
        android:layout_marginBottom="@dimen/fab_margin" />

    <Button
        android:id="@+id/mapsFragment_btnZoomMyMarker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/mapsFragment_markersSpinner"
        android:text="zoom my marker" />

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_alignParentBottom="true"
        android:theme="@style/Theme.FindPet"
        tools:context=".Fragments.MapsFragment" />
</RelativeLayout>
